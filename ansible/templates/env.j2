# ==========================================
# Variables de entorno – Producción debian2
# ==========================================

# Puertos de cada servicio
PORT=3000

# JWT (compartido entre servicios)
JWT_SECRET={{ jwt_secret | default('changeme-prod-secret') }}

# URLs de los microservicios usando IP pública de debian2
# (el api-gateway se comunica con los otros servicios por IP del host)
USERS_SERVICE_URL=http://192.168.122.6:3001
ACADEMIC_SERVICE_URL=http://192.168.122.6:3002

# URL pública del API Gateway (usada por el frontend en el browser)
VITE_API_URL=http://192.168.122.6:3000
VITE_APP_NAME=GestionAcademica
